DATABASE_URL=postgres://postgres:postgres@localhost:5432/industryquest
BETTER_AUTH_SECRET=change-me-32-chars-minimum-random-string
BETTER_AUTH_URL=http://localhost:3000
# Optional: when unset, emails are printed to the server console.
RESEND_API_KEY=
EMAIL_FROM=IndustryQuest <noreply@example.com>
# Demo mode: fictional-data banner and one-click sign-in as seeded personas. Never enable in production.
DEMO_MODE=1
# Where students go when nothing fits or they need an equivalency review.
COORDINATOR_EMAIL=staff@demo.test
